\documentclass[pdf]{beamer}
\mode<all>{
  \usetheme{Madrid}
  \usecolortheme{dove}
  \useoutertheme{split}
}
\graphicspath{ {./images/} }
\mode<handout>{\usecolortheme{dolphin}}
\setbeamertemplate{itemize items}[default]
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{graphicx}
\usepackage{url}
\usepackage{colortbl}
\usepackage{epstopdf}
\usepackage{wrapfig}
\usepackage[normalem]{ulem}
\usepackage{pgf, tikz}
\usepackage{listings}
\usetikzlibrary{arrows, automata}

\usenavigationsymbolstemplate{}

% title information
\title{A Title Here}
\author{Andy Scott}

\begin{document}

%%%%%%%%%%%%%%%%%%%%%%%%

\frame{\titlepage}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{preface}

\subsection{you \& your garden}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}
  \begin{center}
    This is a slide
    $$\cdots$$
    \pause
    This is more of a slide
  \end{center}
\end{frame}

\begin{frame}
  \begin{center}
    \tikzset{nodeA/.style={}}
    \only<5>{\tikzset{nodeA/.style={fill = green}}}
    \tikzset{nodeB/.style={}}
    \only<6>{\tikzset{nodeB/.style={fill = green}}}
    \tikzset{nodeC/.style={}}
    \only<6>{\tikzset{nodeC/.style={fill = green}}}
    \tikzset{nodeD/.style={}}
    \only<7->{\tikzset{nodeD/.style={fill = green}}}
    \begin{tikzpicture}[
        > = stealth,
        shorten > = 1pt,
        auto,
        node distance = 1.5cm,
        semithick
      ]
      \tikzstyle{every state}=[
        draw = black,
        thin,
        fill = white,
        minimum size = 4mm
      ]
      \node[state, nodeA] (a) {$a$};
      \node[state, nodeB] (b) [below left of=a] {$b$};
      \node[state, nodeC] (c) [below right of=a] {$c$};
      \node[state] (e) [below of=c] {$e$};
      \node[state, nodeD] (d) [left of=e] {$d$};
      \path[->] (b) edge node {} (a);
      \path[->] (c) edge node {} (a);
      \path[->] (d) edge node {} (b);
      \path[->] (d) edge node {} (c);
      \path[->] (e) edge node {} (c);
    \end{tikzpicture}
  \end{center}
\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}
  \frametitle{questions?}
  \pause
  questions?
\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\end{document}
